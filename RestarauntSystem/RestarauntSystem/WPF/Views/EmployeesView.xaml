<UserControl x:Class="RestarauntSystem.WPF.Views.EmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestarauntSystem.WPF.Views"
             xmlns:converters="clr-namespace:RestarauntSystem.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:NullToTextConverter x:Key="NullToTextConverter"/>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0 0 0 10">
            <Button Content="Обновить список" Command="{Binding LoadEmployeesCommand}" Margin="0 0 10 0"/>
        </StackPanel>

        <!-- Список сотрудников -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmployee}"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО" Width="*">
                    <DataGridTextColumn.Binding>
                        <MultiBinding StringFormat="{}{0} {1} {2}">
                            <Binding Path="LastName"/>
                            <Binding Path="FirstName"/>
                            <Binding Path="MiddleName"/>
                        </MultiBinding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Должность" Binding="{Binding Position.PositionName}" Width="Auto"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="Auto"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Форма добавления/редактирования -->
        <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="0,1,0,0" Margin="0 10 0 0" Padding="0 10 0 0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="{Binding SelectedEmployee, Converter={StaticResource NullToTextConverter}, 
          ConverterParameter='Добавить сотрудника|Редактировать сотрудника'}" 
          FontWeight="Bold" Margin="0 0 0 5"/>
                    
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Фамилия"/>
                        <TextBox Text="{Binding NewEmployeeLastName}" Width="120" Margin="0 0 5 5"/>
                        <Label Content="Имя"/>
                        <TextBox Text="{Binding NewEmployeeFirstName}" Width="120" Margin="0 0 5 5"/>
                        <Label Content="Отчество"/>
                        <TextBox Text="{Binding NewEmployeeMiddleName}" Width="120" Margin="0 0 5 5"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal">
                        <DatePicker SelectedDate="{Binding NewEmployeeBirthDate}" Width="150" Margin="0 0 5 5"/>
                        <ComboBox ItemsSource="{Binding Positions}" DisplayMemberPath="PositionName"
                                  SelectedItem="{Binding SelectedPosition}" Width="200" Margin="0 0 5 5"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Телефон"/>
                        <TextBox Text="{Binding NewEmployeePhone}" Width="150" Margin="0 0 5 5"/>
                        <Label Content="Email"/>
                        <TextBox Text="{Binding NewEmployeeEmail}" Width="200" Margin="0 0 5 5"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Right">
                    <Button Content="Добавить" Command="{Binding AddEmployeeCommand}" Margin="0 0 0 10" Padding="15 5"/>
                    <Button Content="Обновить" Command="{Binding UpdateEmployeeCommand}" Margin="0 0 0 10" Padding="15 5"/>
                    <Button Content="Удалить" Command="{Binding DeleteEmployeeCommand}" Padding="15 5" Background="#FFFFDDDD"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
