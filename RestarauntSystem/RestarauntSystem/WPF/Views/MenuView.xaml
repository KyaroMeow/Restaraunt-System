<UserControl x:Class="RestarauntSystem.WPF.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converter="clr-namespace:RestarauntSystem.WPF.Converters"
             xmlns:local="clr-namespace:RestarauntSystem.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converter:DishCategoryIdToText x:Key="DishCategoryIdToText"/>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0 0 0 10">
            <Button Content="Обновить меню" Command="{Binding LoadMenuCommand}" Margin="0 0 10 0"/>
        </StackPanel>

        <!-- Список блюд -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Dishes}" SelectedItem="{Binding SelectedDish}"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding DishName}" Width="Auto"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding CategoryId, Converter={StaticResource DishCategoryIdToText}}" Width="Auto"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat={}{0:C}}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Форма добавления/редактирования -->
        <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="0,1,0,0" Margin="0 10 0 0" Padding="0 10 0 0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Добавить новое блюдо:" FontWeight="Bold" Margin="0 0 0 5"/>
                    <Label Content="Название блюда"/>
                    <TextBox Text="{Binding NewDishName}" Margin="0 0 0 5"/>
                    <Label Content="Описание"/>
                    <TextBox Text="{Binding NewDishDescription}" Margin="0 0 0 5" 
                              AcceptsReturn="True" Height="60"/>
                    <StackPanel Orientation="Horizontal">
                        <ComboBox ItemsSource="{Binding Categories}" DisplayMemberPath="CategoryName"
                                  SelectedItem="{Binding SelectedCategory}" Width="200" Margin="0 0 10 0"/>
                        <Label Content="Цена"/>
                        <TextBox Text="{Binding NewDishPrice}" Width="100"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Right">
                    <Button Content="Добавить" Command="{Binding AddDishCommand}" Margin="0 0 0 10" Padding="15 5"/>
                    <Button Content="Удалить" Command="{Binding DeleteDishCommand}" Padding="15 5" Background="#FFFFDDDD"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
