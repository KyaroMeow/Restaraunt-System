<UserControl x:Class="RestarauntSystem.WPF.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestarauntSystem.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0 0 0 10">
            <Button Content="Обновить инвентарь" Command="{Binding LoadInventoryCommand}" Margin="0 0 10 0"/>
        </StackPanel>

        <!-- Список инвентаря -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding InventoryItems}" SelectedItem="{Binding SelectedInventoryItem}"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Продукт" Binding="{Binding Product.ProductName}" Width="Auto"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="Auto"/>
                <DataGridTextColumn Header="Ед. измерения" Binding="{Binding Product.Unit}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Форма управления запасами -->
        <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="0,1,0,0" Margin="0 10 0 0" Padding="0 10 0 0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Управление запасами:" FontWeight="Bold" Margin="0 0 0 5"/>
                    <TextBlock Text="{Binding SelectedInventoryItem.Product.ProductName}" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" Margin="0 5 0 0">
                        <Label Content="Изменение"/>
                        <TextBox Text="{Binding StockAdjustment}" Width="100" Margin="0 0 5 0"/>
                        <Button Content="Применить" Command="{Binding UpdateStockCommand}" Padding="10 5"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="20 0 0 0">
                    <TextBlock Text="Заказ у поставщика:" FontWeight="Bold" Margin="0 0 0 5"/>
                    <ComboBox ItemsSource="{Binding Suppliers}" DisplayMemberPath="SupplierName"
                              SelectedItem="{Binding SelectedSupplier}" Width="200" Margin="0 0 0 5"/>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Количество"/>
                        <TextBox Text="{Binding OrderQuantity}" Width="100" Margin="0 0 5 0" />
                        <Button Content="Заказать" Command="{Binding OrderSuppliesCommand}" Padding="10 5"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="20 0 0 0">
                    <TextBlock Text="Критический запас:" FontWeight="Bold" Margin="0 0 0 5"/>
                    <ListBox ItemsSource="{Binding LowStockItems}" Height="100" Width="250">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding ProductName}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Quantity, StringFormat={}{0} осталось}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
